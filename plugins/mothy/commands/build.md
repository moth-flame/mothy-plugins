---
description: Decompose a plan into work units and build them with parallel sub-agents (red-green TDD + adversarial verification)
argument-hint: "[topic]"
---

Invoke the build skill to execute the plan / build $ARGUMENTS.
